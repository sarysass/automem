---
phase: 11-deep-user-workflow-and-scope-regression
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_deep_user_memory_flows.py
  - backend/main.py
autonomous: true
requirements:
  - E2E-01
  - E2E-02
must_haves:
  truths:
    - "Maintainer can run named memory and fact stories from submission through retrieval and visible state change."
    - "Supersede and conflict-review stories prove current-first retrieval plus history visibility together, not as disconnected assertions."
    - "If explanation text is added, it must be grounded in the real ranking reasons already emitted by search logic."
  artifacts:
    - path: tests/test_deep_user_memory_flows.py
      provides: Named deep-user memory and fact workflow regressions
    - path: backend/main.py
      provides: Any thin helper needed to turn real search signals into stable story assertions
  key_links:
    - from: tests/test_deep_user_memory_flows.py
      to: /memories and /search
      via: API-first story assertions over write response, list state, and search state
      pattern: fact_action / fact_status / include_history
    - from: tests/test_deep_user_memory_flows.py
      to: backend/main.py
      via: search explainability and reranking signals
      pattern: finalize_search_result / rerank_results / classify_query_intent
---

<objective>
Build named deep-user memory and fact workflow coverage that reads like the user stories Phase 11 is meant to protect.

Purpose: Satisfy `E2E-01` and `E2E-02` with dedicated, maintainer-readable scenario tests instead of burying the stories inside baseline regression files.
Output: A dedicated memory/fact scenario suite and only the thinnest production helper changes required to make the stories stable and truthful.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-deep-user-workflow-and-scope-regression/11-CONTEXT.md
@.planning/phases/11-deep-user-workflow-and-scope-regression/11-RESEARCH.md
@.planning/phases/11-deep-user-workflow-and-scope-regression/11-PATTERNS.md
@tests/conftest.py
@tests/test_backend_baseline.py
@backend/main.py

<interfaces>
From `backend/main.py`:

```python
result["explainability"] = {
    "matched_by": matched_by,
    "matched_fields": matched_fields,
    "matched_terms": matched_terms,
    "status": status,
    ...
}
```

```python
history = client.post("/search", json={"query": "...", "include_history": True})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create named deep-user memory stories for intent recall and fact lifecycle</name>
  <read_first>
.planning/phases/11-deep-user-workflow-and-scope-regression/11-CONTEXT.md
.planning/phases/11-deep-user-workflow-and-scope-regression/11-RESEARCH.md
tests/test_backend_baseline.py
backend/main.py
  </read_first>
  <files>tests/test_deep_user_memory_flows.py</files>
  <acceptance_criteria>
- The new suite contains named stories for at least: intent-style preference recall, current-first supersede behavior with history trace, and conflict-review preserving the active fact.
- Each story proves the full workflow from POST `/memories` through GET `/memories` or POST `/search` and the final observable state.
- At least one story asserts that intent-style recall lands the expected current result in the top 3, matching the discuss-approved retrieval threshold.
  </acceptance_criteria>
  <behavior>
    - Test 1: A preference update makes the new fact active, keeps the old one as history, and default search surfaces only the active fact.
    - Test 2: A project-context conflict leaves the active fact in place and exposes the competing fact only through review/history-aware retrieval.
  </behavior>
  <action>Create `tests/test_deep_user_memory_flows.py` as a story-first suite that reuses the existing Phase 10/Phase 11 backend fixtures. Lift setup ideas from `tests/test_backend_baseline.py`, but rename and regroup them around the named workflows the maintainer cares about rather than endpoint-by-endpoint helper checks.</action>
  <verify>
    <automated>uv run pytest tests/test_deep_user_memory_flows.py -x</automated>
  </verify>
  <done>The repo has a dedicated fast-lane memory/fact regression suite that directly proves the highest-value deep-user stories.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make retrieval explanation assertions truthful without inventing a second ranking system</name>
  <read_first>
.planning/phases/11-deep-user-workflow-and-scope-regression/11-DISCUSSION-LOG.md
.planning/phases/11-deep-user-workflow-and-scope-regression/11-SCOPE-MODEL.md
backend/main.py
tests/test_deep_user_memory_flows.py
  </read_first>
  <files>backend/main.py, tests/test_deep_user_memory_flows.py</files>
  <acceptance_criteria>
- Story assertions can point to the true winning signals for at least one core query without fabricating reasons.
- If new explanation text or fields are added, they are derived from existing query-intent, matched-field, and fact-status signals.
- No separate fake explanation layer or hardcoded per-test explanation text is introduced.
  </acceptance_criteria>
  <behavior>
    - Test 1: The winning result exposes enough evidence to explain why it ranked first.
    - Test 2: History and non-winning candidates do not cause an explanation that contradicts the actual result ordering.
  </behavior>
  <action>Use the current `explainability` payload if it is already sufficient. If it is too raw for stable named-story assertions, add only a thin helper in `backend/main.py` that summarizes the real ranking reasons into short, evidence-based output. Keep the helper coupled to the existing reranking signals rather than creating parallel logic.</action>
  <verify>
    <automated>uv run pytest tests/test_deep_user_memory_flows.py -x</automated>
  </verify>
  <done>Core memory stories can assert truthful explanation quality without introducing a second or misleading ranking model.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test stories -> retrieval ranking | Tests must validate the actual winning signals, not a fabricated narrative layer |
| fact history -> default search | Default search must not leak superseded or review facts into current-first stories unless the story explicitly requests history |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | T/I | `backend/main.py` explanation output | mitigate | Derive any added explanation text from the existing explainability/rerank signals instead of hand-built string logic disconnected from ranking. |
| T-11-01-02 | I | fact lifecycle search stories | mitigate | Assert both default search and `include_history=True` behavior so a history leak or missing current fact fails visibly. |
</threat_model>

<verification>
- `uv run pytest tests/test_deep_user_memory_flows.py -x`
</verification>

<success_criteria>
- Named deep-user memory stories exist and are easy to scan.
- Fact supersede and conflict-review are proven through complete API workflows.
- Explanation checks, if present, stay grounded in real ranking signals.
</success_criteria>

<output>
After completion, create `.planning/phases/11-deep-user-workflow-and-scope-regression/11-01-SUMMARY.md`
</output>
