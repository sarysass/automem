---
phase: 11-deep-user-workflow-and-scope-regression
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_deep_user_task_flows.py
  - tests/test_task_governance_targets.py
  - backend/governance/task_policy.py
  - backend/main.py
autonomous: true
requirements:
  - E2E-01
  - E2E-03
  - UNIT-01
must_haves:
  truths:
    - "Task coverage proves real handoff continuity, not merely that one summary can be stored."
    - "Cleanup regressions stay story-shaped: noise is archived or pruned without hiding genuine work."
    - "Task classification and materialization edge cases fail locally in unit tests."
  artifacts:
    - path: tests/test_deep_user_task_flows.py
      provides: Named task lifecycle and cleanup stories
    - path: tests/test_task_governance_targets.py
      provides: Edge-case task policy and cleanup rule coverage
    - path: backend/governance/task_policy.py
      provides: Minimal rule adjustments if story or unit failures expose classification drift
  key_links:
    - from: tests/test_deep_user_task_flows.py
      to: /task-summaries /tasks /tasks/normalize
      via: multi-hop handoff, retrieval, and cleanup assertions
      pattern: task identity + next action + current state
    - from: tests/test_task_governance_targets.py
      to: backend/governance/task_policy.py
      via: direct classification and materialization checks
      pattern: classify_task_kind / should_materialize_task
---

<objective>
Build the named task-side stories for handoff continuity, cleanup, and governance rules that Phase 11 says are high value.

Purpose: Satisfy `E2E-03` and `UNIT-01` while keeping the phase focused on deep-user workflows instead of raw helper coverage alone.
Output: A dedicated task workflow regression suite and targeted rule/unit coverage for task governance.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-deep-user-workflow-and-scope-regression/11-CONTEXT.md
@.planning/phases/11-deep-user-workflow-and-scope-regression/11-RESEARCH.md
@.planning/phases/11-deep-user-workflow-and-scope-regression/11-PATTERNS.md
@tests/test_backend_baseline.py
@tests/test_task_governance_targets.py
@backend/governance/task_policy.py
@backend/main.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove multi-hop handoff continuity through named task stories</name>
  <read_first>
.planning/phases/11-deep-user-workflow-and-scope-regression/11-DISCUSSION-LOG.md
tests/test_backend_baseline.py
backend/main.py
  </read_first>
  <files>tests/test_deep_user_task_flows.py</files>
  <acceptance_criteria>
- The new task suite contains at least one story where a first handoff is stored, a second step continues it, and final retrieval still exposes task identity, current state, and next action.
- The suite uses public task and search surfaces rather than hidden helper-only assertions.
- Story names reflect the workflow being protected, not generic endpoint actions.
  </acceptance_criteria>
  <behavior>
    - Test 1: A task summary creates or updates a materialized task and the next retrieval step can continue it without rereading raw history.
    - Test 2: A follow-up summary preserves task identity while advancing state and next action.
  </behavior>
  <action>Create `tests/test_deep_user_task_flows.py` and group the strongest existing task flows into multi-hop named stories. Keep the focus on continuity, not volume: the story should prove the next action, current state, and task identity survive across more than one hop.</action>
  <verify>
    <automated>uv run pytest tests/test_deep_user_task_flows.py -x</automated>
  </verify>
  <done>The repo has story-level proof that a downstream agent can realistically pick up the right task and continue it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Turn cleanup and normalization into explicit regression stories</name>
  <read_first>
tests/test_backend_baseline.py
backend/main.py
  </read_first>
  <files>tests/test_deep_user_task_flows.py, backend/main.py</files>
  <acceptance_criteria>
- At least one story proves obvious system noise is archived or pruned.
- At least one story proves legitimate work remains visible after cleanup.
- The resulting assertions use the public normalize/archive/list surfaces and their observable counters or state changes.
  </acceptance_criteria>
  <behavior>
    - Test 1: System-noise tasks are archived or pruned through `/tasks/normalize`.
    - Test 2: Real work that still has backing task memory remains active and retrievable.
  </behavior>
  <action>Refactor existing normalize scenarios into named stories in `tests/test_deep_user_task_flows.py`. Only change `backend/main.py` if the stories reveal a real cleanup bug or the current endpoint response does not expose enough stable state for the regression story to be trustworthy.</action>
  <verify>
    <automated>uv run pytest tests/test_deep_user_task_flows.py -x</automated>
  </verify>
  <done>Cleanup behavior is proven by realistic task stories rather than scattered helper checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deepen task governance unit coverage for classification and suppression edges</name>
  <read_first>
backend/governance/task_policy.py
tests/test_task_governance_targets.py
  </read_first>
  <files>tests/test_task_governance_targets.py, backend/governance/task_policy.py</files>
  <acceptance_criteria>
- Unit tests cover conversational/meta, obvious system noise, project-bound work, snapshot-like titles, and materialization suppression edges.
- Failures localize to task policy rather than requiring a whole API-story debug session.
- Any production rule changes stay minimal and are explained by a concrete failing test.
  </acceptance_criteria>
  <behavior>
    - Test 1: Meta/system/noise content does not materialize into actionable tasks.
    - Test 2: Legitimate work content still materializes and stores task memory.
  </behavior>
  <action>Extend `tests/test_task_governance_targets.py` with direct rule matrices around `classify_task_kind`, `should_materialize_task`, and task-memory suppression. Touch `backend/governance/task_policy.py` only if a failing edge case reveals real drift from the discuss-approved task philosophy.</action>
  <verify>
    <automated>uv run pytest tests/test_task_governance_targets.py tests/test_deep_user_task_flows.py -x</automated>
  </verify>
  <done>Task rule regressions fail in focused unit tests before they can corrupt the higher-level handoff stories.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| task summaries -> materialized tasks | Noisy or conversational content must not become actionable work by mistake |
| cleanup logic -> active work visibility | Cleanup must remove noise without erasing valid work or its backing task memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | T/I | `backend/governance/task_policy.py` | mitigate | Add focused unit coverage for the exact title and summary patterns that can drift between work, meta, system, and snapshot. |
| T-11-02-02 | D | `/tasks/normalize` regressions | mitigate | Named cleanup stories must assert both removal of noise and survival of real work so over-aggressive cleanup fails visibly. |
</threat_model>

<verification>
- `uv run pytest tests/test_deep_user_task_flows.py -x`
- `uv run pytest tests/test_task_governance_targets.py -x`
</verification>

<success_criteria>
- Multi-hop handoff continuity is proven by story-shaped tests.
- Cleanup regressions are visible through public task surfaces.
- Task policy edges are localized by fast unit tests.
</success_criteria>

<output>
After completion, create `.planning/phases/11-deep-user-workflow-and-scope-regression/11-02-SUMMARY.md`
</output>
