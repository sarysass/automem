---
phase: 11-deep-user-workflow-and-scope-regression
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_identity_e2e.py
  - tests/test_identity_unit.py
  - backend/main.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - UNIT-02
must_haves:
  truths:
    - "Fail-closed scope behavior is proven across memory, task, search, close, archive, and forget, not just one representative route."
    - "Single-project, multi-project, and admin paths are all covered against the same deep-user surfaces."
    - "Hidden-resource semantics remain endpoint-faithful: explicit scope violations fail loudly, while inaccessible foreign resources stay hidden."
  artifacts:
    - path: tests/test_identity_e2e.py
      provides: Expanded end-to-end authorization and scope regression matrix
    - path: tests/test_identity_unit.py
      provides: Focused helper-level scope enforcement coverage
    - path: backend/main.py
      provides: Minimal helper fixes if the expanded matrix reveals real fail-open or inconsistent behavior
  key_links:
    - from: tests/test_identity_e2e.py
      to: /agent-keys and public memory/task routes
      via: scoped non-admin keys versus admin baseline
      pattern: single-project / multi-project / admin
    - from: tests/test_identity_unit.py
      to: backend/main.py
      via: direct scope helper checks
      pattern: enforce_project_identity / merge_project_id_into_metadata / ensure_memory_item_access
---

<objective>
Expand the current identity tests into the full fail-closed scope matrix Phase 11 requires.

Purpose: Satisfy `AUTH-01`, `AUTH-02`, and the current-scope part of `UNIT-02` through API-first regression coverage plus focused helper tests.
Output: A complete scope matrix across memory and task surfaces, with helper-level localization for the current shipped enforcement rules.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-deep-user-workflow-and-scope-regression/11-CONTEXT.md
@.planning/phases/11-deep-user-workflow-and-scope-regression/11-SCOPE-MODEL.md
@.planning/phases/11-deep-user-workflow-and-scope-regression/11-RESEARCH.md
@.planning/phases/11-deep-user-workflow-and-scope-regression/11-PATTERNS.md
@tests/test_identity_e2e.py
@tests/test_identity_unit.py
@backend/main.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand the end-to-end auth matrix across all relevant memory and task surfaces</name>
  <read_first>
tests/test_identity_e2e.py
backend/main.py
.planning/phases/11-deep-user-workflow-and-scope-regression/11-CONTEXT.md
  </read_first>
  <files>tests/test_identity_e2e.py</files>
  <acceptance_criteria>
- The matrix covers single-project, multi-project, and admin paths across memory write/search/read/delete and task create/list/read/close/archive.
- The suite asserts the correct distinction between missing explicit project selection, forbidden explicit scope, and hidden foreign resources.
- The file remains readable and centered on the existing scoped-key helper pattern.
  </acceptance_criteria>
  <behavior>
    - Test 1: Multi-project keys must explicitly choose project scope where the current contract requires it.
    - Test 2: A scoped key cannot read, close, archive, or forget foreign-project resources even when it knows the resource ID.
  </behavior>
  <action>Extend `tests/test_identity_e2e.py` in place. Reuse the current scoped key helper, preserve its style, and cover the remaining missing API surfaces rather than creating a second auth suite. Prefer explicit named tests over one giant parameter matrix when the failure message would be clearer.</action>
  <verify>
    <automated>uv run pytest tests/test_identity_e2e.py -x</automated>
  </verify>
  <done>The repo has one authoritative fail-closed end-to-end scope matrix for the shipped model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Localize scope helper regressions in focused unit tests</name>
  <read_first>
tests/test_identity_unit.py
backend/main.py
  </read_first>
  <files>tests/test_identity_unit.py, backend/main.py</files>
  <acceptance_criteria>
- Helper tests cover the current project enforcement seams that are most likely to drift.
- At minimum, tests cover normalization, single-project defaulting, multi-project explicit selection, merge conflict rejection, and any direct-access helper logic added for clarity.
- Production changes are limited to helper fixes or extra pure seams needed for unit localization.
  </acceptance_criteria>
  <behavior>
    - Test 1: Project enforcement helpers reject foreign explicit scopes and conflicting metadata or filters.
    - Test 2: Direct resource-access helpers hide inaccessible project-scoped resources from scoped keys.
  </behavior>
  <action>Extend `tests/test_identity_unit.py` with focused helper coverage and touch `backend/main.py` only where the new matrix reveals an inconsistency or where extracting a tiny pure seam makes the fail-closed behavior much easier to test and keep stable.</action>
  <verify>
    <automated>uv run pytest tests/test_identity_unit.py tests/test_identity_e2e.py -x</automated>
  </verify>
  <done>Scope failures can be diagnosed locally at the helper level instead of only through end-to-end matrix failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-admin scoped key -> memory/task surfaces | The API must fail closed across every route, not only the most common ones |
| direct resource lookup -> hidden-resource behavior | A caller that knows an ID must not learn foreign-project existence through permissive status codes or leaked payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | I/E | `backend/main.py` scope helpers | mitigate | Expand API and unit coverage across all scope-sensitive routes and keep status-code expectations endpoint-faithful. |
| T-11-03-02 | I | direct resource access helpers | mitigate | Add focused tests for `ensure_memory_item_access` and `ensure_task_row_access` behavior wherever practical so 404-hiding cannot silently regress. |
</threat_model>

<verification>
- `uv run pytest tests/test_identity_e2e.py -x`
- `uv run pytest tests/test_identity_unit.py -x`
</verification>

<success_criteria>
- The shipped scope model is proven fail-closed across all major surfaces.
- Helper-level tests quickly pinpoint current-scope regressions.
- The suite preserves the distinction between explicit-scope errors and hidden foreign resources.
</success_criteria>

<output>
After completion, create `.planning/phases/11-deep-user-workflow-and-scope-regression/11-03-SUMMARY.md`
</output>
