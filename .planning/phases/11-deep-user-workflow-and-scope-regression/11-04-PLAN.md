---
phase: 11-deep-user-workflow-and-scope-regression
plan: "04"
type: execute
wave: 2
depends_on:
  - "11-01"
  - "11-03"
files_modified:
  - tests/test_scope_model_unit.py
  - backend/main.py
autonomous: true
requirements:
  - UNIT-02
must_haves:
  truths:
    - "Phase 11 prepares the approved scope-model direction through pure helper seams instead of forcing a full public contract migration."
    - "Legacy scope classification and mixed-retrieval role decisions are testable, deterministic, and narrow."
    - "Ambiguous legacy records can land in `migration_review` instead of being silently misclassified."
  artifacts:
    - path: tests/test_scope_model_unit.py
      provides: Focused helper coverage for scope-model preparation
    - path: backend/main.py
      provides: Pure helper seams for legacy-record classification and any supporting explanation-role decisions
  key_links:
    - from: tests/test_scope_model_unit.py
      to: .planning/phases/11-deep-user-workflow-and-scope-regression/11-SCOPE-MODEL.md
      via: direct assertions over approved evidence tiers and migration-review fallback
      pattern: user_global / project / migration_review
    - from: backend/main.py
      to: tests/test_deep_user_memory_flows.py
      via: any thin helper used to stabilize explanation-role or scope classification assertions
      pattern: dominant intent + supporting context
---

<objective>
Lay the smallest useful implementation foundation for the approved scope model inside Phase 11 without bundling the full API migration.

Purpose: Satisfy the future-facing helper part of `UNIT-02` and keep Phase 11 aligned with the approved `user_global` versus `project` direction.
Output: Pure helper seams and unit tests for legacy-record scope classification and mixed-retrieval role decisions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-deep-user-workflow-and-scope-regression/11-SCOPE-MODEL.md
@.planning/phases/11-deep-user-workflow-and-scope-regression/11-CONTEXT.md
@.planning/phases/11-deep-user-workflow-and-scope-regression/11-RESEARCH.md
@backend/main.py
@tests/test_identity_unit.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pure helper seams for legacy scope classification using the approved evidence tiers</name>
  <read_first>
.planning/phases/11-deep-user-workflow-and-scope-regression/11-SCOPE-MODEL.md
backend/main.py
  </read_first>
  <files>backend/main.py, tests/test_scope_model_unit.py</files>
  <acceptance_criteria>
- The helper logic can classify legacy records into `project`, `user_global`, or `migration_review`.
- Hard structural evidence outranks semantic hints, matching the approved evidence-tier rule.
- Ambiguous records with weak evidence do not get silently forced into one side.
  </acceptance_criteria>
  <behavior>
    - Test 1: Explicit project/task evidence routes a legacy record to `project`.
    - Test 2: Stable preference/profile or project-overview evidence routes a legacy record to `user_global`.
    - Test 3: Weak or conflicting evidence falls into `migration_review`.
  </behavior>
  <action>Add narrow pure helpers in `backend/main.py` or a tightly adjacent helper section that implement the approved evidence-tier model from `11-SCOPE-MODEL.md`. Cover them with direct unit tests in `tests/test_scope_model_unit.py`. Do not switch the public API contract in this plan.</action>
  <verify>
    <automated>uv run pytest tests/test_scope_model_unit.py -x</automated>
  </verify>
  <done>The repo has deterministic helper coverage for future legacy-scope migration decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add pure helper coverage for mixed-retrieval role decisions when it improves explanation truthfulness</name>
  <read_first>
.planning/phases/11-deep-user-workflow-and-scope-regression/11-DISCUSSION-LOG.md
.planning/phases/11-deep-user-workflow-and-scope-regression/11-SCOPE-MODEL.md
backend/main.py
  </read_first>
  <files>backend/main.py, tests/test_scope_model_unit.py</files>
  <acceptance_criteria>
- If a helper is introduced, it chooses a main answer by dominant query intent and marks the other side as supporting context.
- Helper output is role-oriented and does not require exposing raw internal scope labels in user-facing text by default.
- No broad retrieval refactor is introduced unless a direct Phase 11 test requires it.
  </acceptance_criteria>
  <behavior>
    - Test 1: Preference or identity intent selects global-like context as the main answer role.
    - Test 2: Task-status or blocker intent selects project execution state as the main answer role.
  </behavior>
  <action>Only if it materially clarifies Phase 11 story assertions, add a tiny pure helper for dominant-intent versus supporting-context decisions. Keep it helper-first and unit-tested. If the current code already provides enough stable signal, leave production logic unchanged and encode the decision table purely in tests or docs.</action>
  <verify>
    <automated>uv run pytest tests/test_scope_model_unit.py -x</automated>
  </verify>
  <done>Future mixed-retrieval explanation decisions are either explicitly unit-tested or consciously deferred without bloating this phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy data -> new scope model | Automatic migration logic can silently misclassify records if evidence tiers are fuzzy |
| mixed retrieval -> explanation roles | Role selection can become misleading if it diverges from the actual dominant intent decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | T/I | legacy scope classifier | mitigate | Encode the approved evidence hierarchy in pure helper tests and preserve `migration_review` as the ambiguity sink. |
| T-11-04-02 | I | mixed-retrieval role helper | mitigate | Keep any role-selection helper tiny, deterministic, and grounded in the approved query-intent table instead of freeform heuristics. |
</threat_model>

<verification>
- `uv run pytest tests/test_scope_model_unit.py -x`
</verification>

<success_criteria>
- Scope-model preparation is captured in testable pure helpers instead of vague future notes.
- Ambiguous legacy records can be routed to `migration_review`.
- Phase 11 stays aligned with the approved scope model without attempting the full API cutover.
</success_criteria>

<output>
After completion, create `.planning/phases/11-deep-user-workflow-and-scope-regression/11-04-SUMMARY.md`
</output>
