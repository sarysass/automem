---
phase: 10-test-harness-and-lane-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - 10-01
  - 10-02
files_modified:
  - pyproject.toml
  - tests/test_lane_selection.py
  - CONTRIBUTING.md
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - HARN-03
must_haves:
  truths:
    - "Maintainer can choose a fast default pytest lane and an explicit slow live-harness lane."
    - "Routine validation stays on the fast lane while deeper live-process coverage is still runnable by name."
  artifacts:
    - path: pyproject.toml
      provides: Strict pytest marker registration and subprocess-aware coverage/lane config
    - path: tests/test_lane_selection.py
      provides: Executable proof that fast/slow lane configuration points at the intended suites
    - path: CONTRIBUTING.md
      provides: Contributor-facing commands for fast and slow lanes
  key_links:
    - from: pyproject.toml
      to: tests/test_harness_foundation_live.py
      via: slow and serial marker registration
      pattern: strict-markers
    - from: .github/workflows/ci.yml
      to: pytest
      via: fast-lane PR command
      pattern: pytest -m "not slow"
---

<objective>
Wire the new harness into explicit fast and slow lanes so default feedback stays quick while deeper coverage remains available.

Purpose: Satisfy HARN-03 with real marker registration, lane verification, and contributor-facing commands that point at the new live suites.
Output: Pytest lane config, a config-verification test, and updated contributor/CI commands for fast versus slow execution.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-test-harness-and-lane-foundation/10-VALIDATION.md
@pyproject.toml
@CONTRIBUTING.md
@.github/workflows/ci.yml
@tests/test_harness_foundation_live.py
@tests/test_runtime_entrypoints_live.py

<interfaces>
Phase 10 validation target commands:

```text
uv run pytest -m "not slow"
uv run pytest -m slow tests/test_harness_foundation_live.py tests/test_runtime_entrypoints_live.py
```

Current pytest config:

```toml
[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register strict fast and slow lane config with an executable lane-selection test</name>
  <read_first>
.planning/phases/10-test-harness-and-lane-foundation/10-VALIDATION.md
pyproject.toml
tests/test_harness_foundation_live.py
tests/test_runtime_entrypoints_live.py
  </read_first>
  <files>pyproject.toml, tests/test_lane_selection.py</files>
  <acceptance_criteria>
- `pyproject.toml` adds `--strict-markers`, registers `slow` and `serial` markers, and configures subprocess-aware coverage for child-process live tests.
- `tests/test_lane_selection.py` parses `pyproject.toml` and verifies the marker declarations plus `@pytest.mark.slow` / `@pytest.mark.serial` usage on `tests/test_harness_foundation_live.py` and `tests/test_runtime_entrypoints_live.py`.
- The default fast-lane command is `uv run pytest -m "not slow"` and the documented slow-lane command targets `tests/test_harness_foundation_live.py` and `tests/test_runtime_entrypoints_live.py`.
  </acceptance_criteria>
  <behavior>
    - Test 1: Config verification fails if `slow` or `serial` markers are missing from `pyproject.toml`.
    - Test 2: Config verification fails if the Phase 10 live suites are not marked `slow` and `serial`.
  </behavior>
  <action>Update `pyproject.toml` to set `addopts = "-q --strict-markers"`, register `slow` and `serial` marker descriptions, and add coverage settings needed for child-process measurement (for example `[tool.coverage.run] parallel = true` with `patch = ["subprocess"]` if compatible with the installed coverage version). Create `tests/test_lane_selection.py` using `tomllib` and file-text checks so the repo has an executable guard that the marker config and slow-suite targeting do not drift.</action>
  <verify>
    <automated>uv run pytest tests/test_lane_selection.py -x</automated>
  </verify>
  <done>Pytest exposes explicit fast/slow lanes, strict marker enforcement, and a regression test that guards the lane configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Publish fast and slow lane commands in contributor docs and keep PR CI on the fast lane</name>
  <read_first>
.planning/ROADMAP.md
CONTRIBUTING.md
.github/workflows/ci.yml
pyproject.toml
  </read_first>
  <files>CONTRIBUTING.md, .github/workflows/ci.yml</files>
  <acceptance_criteria>
- `CONTRIBUTING.md` documents exact commands for the fast lane (`uv run pytest -m "not slow"`) and the slow live lane (`uv run pytest -m slow tests/test_harness_foundation_live.py tests/test_runtime_entrypoints_live.py`).
- `.github/workflows/ci.yml` keeps the default PR Python validation on the fast lane so the new live suites do not slow routine pull requests.
- If a separate CI slow-lane job is added, it must be explicit and not replace the fast-lane PR job.
  </acceptance_criteria>
  <action>Update `CONTRIBUTING.md` to explain when to use the fast lane versus the slow live lane and to keep the commands in Chinese-first contributor style. Update `.github/workflows/ci.yml` so the existing Python pytest step uses `uv run pytest -m "not slow"`. If you add a dedicated slow-lane CI job, keep it explicitly named and scoped so it does not make every PR behave like the full live harness suite; do not expand into Phase 13 canary policy here.</action>
  <verify>
    <automated>uv run pytest tests/test_lane_selection.py -x && rg -n 'pytest -m \"not slow\"|pytest -m slow' CONTRIBUTING.md .github/workflows/ci.yml pyproject.toml</automated>
  </verify>
  <done>Maintainers and CI both have explicit lane commands, and the default PR loop stays on the fast lane while the slow lane remains available by name.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor or CI command -> pytest selection | A misconfigured command can silently run the wrong lane or skip live-process coverage entirely |
| coverage config -> child processes | Child-process tests can disappear from evidence if coverage/marker wiring is incomplete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | R | `pyproject.toml` and `tests/test_lane_selection.py` | mitigate | Add strict marker registration plus a parsing test so lane selection drift is caught in automation instead of by convention. |
| T-10-03-02 | D | `.github/workflows/ci.yml` | mitigate | Keep the default PR pytest command on `-m "not slow"` so live-process suites do not degrade routine feedback latency. |
| T-10-03-03 | T | `CONTRIBUTING.md` command docs | mitigate | Publish exact fast and slow commands matching the validation plan so maintainers do not invent ad hoc suite selection. |
</threat_model>

<verification>
- `uv run pytest tests/test_lane_selection.py -x`
- `uv run pytest -m "not slow" -x`
- `uv run pytest -m slow tests/test_harness_foundation_live.py tests/test_runtime_entrypoints_live.py -x`
</verification>

<success_criteria>
- `pyproject.toml` registers strict fast/slow lane behavior and subprocess-aware coverage.
- Contributors can run the correct fast and slow commands from `CONTRIBUTING.md`.
- Default PR CI remains on the fast lane while slow live suites remain explicitly available.
</success_criteria>

<output>
After completion, create `.planning/phases/10-test-harness-and-lane-foundation/10-03-SUMMARY.md`
</output>
