---
phase: 10-test-harness-and-lane-foundation
plan: "02"
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - tests/support/runtime_drivers.py
  - tests/test_runtime_entrypoints_live.py
autonomous: true
requirements:
  - HARN-02
must_haves:
  truths:
    - "Maintainer can run the real scheduler and worker scripts against the same isolated live harness."
    - "Runtime-entrypoint tests prove queue state changes through `/governance/jobs`, `/governance/jobs/run-next`, `/metrics`, and `/audit-log`, not through inline helper shortcuts."
  artifacts:
    - path: tests/support/runtime_drivers.py
      provides: Subprocess wrappers for worker and scheduler script entrypoints
    - path: tests/test_runtime_entrypoints_live.py
      provides: Executable HARN-02 coverage through real runtime scripts
  key_links:
    - from: tests/support/runtime_drivers.py
      to: scripts/governance_worker.py
      via: subprocess invocation with MEMORY_URL and MEMORY_API_KEY
      pattern: scripts/governance_worker.py
    - from: tests/support/runtime_drivers.py
      to: scripts/scheduled_consolidate.py
      via: subprocess invocation with enqueue-mode env
      pattern: scripts/scheduled_consolidate.py
---

<objective>
Prove that the real worker and scheduler runtime entrypoints interoperate with the new live harness without test-only backend seams.

Purpose: Satisfy HARN-02 through real script/runtime boundaries instead of inline backend helper calls.
Output: Runtime driver helpers and a slow-lane live suite for scheduler enqueue plus worker consumption.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-test-harness-and-lane-foundation/10-PATTERNS.md
@.planning/phases/10-test-harness-and-lane-foundation/10-VALIDATION.md
@tests/support/live_backend.py
@tests/support/waiting.py
@scripts/governance_worker.py
@scripts/scheduled_consolidate.py
@backend/main.py

<interfaces>
From `scripts/governance_worker.py`:

```python
def build_client() -> httpx.Client:
    api_key = os.environ.get("MEMORY_API_KEY") or os.environ.get("ADMIN_API_KEY", "")
    return httpx.Client(base_url=build_base_url(), headers={"X-API-Key": api_key}, timeout=120.0, trust_env=False)
```

From `scripts/scheduled_consolidate.py`:

```python
def build_mode() -> str:
    return (os.environ.get("MEMORY_CONSOLIDATE_MODE") or "enqueue").strip().lower()
```

From `backend/main.py`:

```python
@app.post("/governance/jobs/run-next")
def governance_jobs_run_next(payload: GovernanceJobRunRequest, auth: dict[str, Any] = Depends(verify_api_key)):
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add subprocess runtime drivers for the real worker and scheduler scripts</name>
  <read_first>
.planning/phases/10-test-harness-and-lane-foundation/10-PATTERNS.md
tests/support/live_backend.py
tests/support/waiting.py
scripts/governance_worker.py
scripts/scheduled_consolidate.py
  </read_first>
  <files>tests/support/runtime_drivers.py</files>
  <acceptance_criteria>
- `tests/support/runtime_drivers.py` launches `scripts/governance_worker.py` and `scripts/scheduled_consolidate.py` as subprocesses, not by importing their `main()` functions into the live tests.
- The scheduler driver passes `MEMORY_URL`, `MEMORY_API_KEY`, `MEMORY_CONSOLIDATE_MODE=enqueue`, and temp lock/idempotency env needed to create deterministic jobs against the harness.
- The worker driver passes `MEMORY_URL`, `MEMORY_API_KEY`, `AUTOMEM_WORKER_ONCE=true`, and a temp `AUTOMEM_WORKER_LOCK_FILE`, then captures stdout JSON and exit status for assertions.
  </acceptance_criteria>
  <behavior>
    - Test 1: Driver returns parsed JSON emitted by `scheduled_consolidate.py` after a successful enqueue.
    - Test 2: Driver returns parsed JSON emitted by `governance_worker.py` after a single `run-next` execution.
  </behavior>
  <action>Create `tests/support/runtime_drivers.py` with small wrappers around `uv run python scripts/scheduled_consolidate.py` and `uv run python scripts/governance_worker.py`. Pass through the live harness base URL and admin key from `tests/support/live_backend.py`, set per-test temp lock files, and surface subprocess stdout/stderr in assertion-friendly return values. Do not call backend queue helpers directly from this module and do not add any production-only endpoint to make the scripts easier to test.</action>
  <verify>
    <automated>uv run python -m py_compile tests/support/runtime_drivers.py</automated>
  </verify>
  <done>The repo has reusable subprocess helpers that exercise the real runtime scripts over the public backend contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove scheduler enqueue and worker run-next flows through the live harness</name>
  <read_first>
.planning/REQUIREMENTS.md
.planning/phases/10-test-harness-and-lane-foundation/10-VALIDATION.md
tests/support/live_backend.py
tests/support/runtime_drivers.py
tests/support/waiting.py
backend/main.py
  </read_first>
  <files>tests/test_runtime_entrypoints_live.py</files>
  <acceptance_criteria>
- `tests/test_runtime_entrypoints_live.py` is marked `@pytest.mark.slow` and `@pytest.mark.serial` and uses the live harness plus `tests/support/runtime_drivers.py` to run `scripts/scheduled_consolidate.py` and `scripts/governance_worker.py` unchanged.
- The suite asserts queue progression with public backend surfaces only: `/governance/jobs`, `/governance/jobs/{job_id}`, `/metrics`, and `/audit-log`.
- The suite uses condition-based waiting helpers from `tests/support/waiting.py`; no hard-coded orchestration sleeps are added to the test body.
  </acceptance_criteria>
  <behavior>
    - Test 1: Scheduler enqueue creates a pending governance job visible via `/governance/jobs/{job_id}`.
    - Test 2: Worker `run-next` processes that job and the suite observes a completed or processed state plus matching metrics/audit evidence.
  </behavior>
  <action>Create `tests/test_runtime_entrypoints_live.py` to seed enough data through the live API for consolidation work, launch `scheduled_consolidate.py` in enqueue mode, wait for the returned `job_id` to become visible, then launch `governance_worker.py` once and poll until the job reaches a terminal processed/completed state. Mark the suite `@pytest.mark.slow` and `@pytest.mark.serial`. Assert returned JSON, `/metrics` governance counters, and `/audit-log?event_type=governance_job_enqueue` or related audit events. Keep the assertions on real script output and real HTTP endpoints; do not satisfy HARN-02 through `run_inline=True` or helper-only backend calls.</action>
  <verify>
    <automated>uv run pytest -m slow tests/test_runtime_entrypoints_live.py -x</automated>
  </verify>
  <done>HARN-02 is proven by live scheduler and worker subprocesses using the shared harness and public backend contracts only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime subprocesses -> backend API | The worker and scheduler scripts send untrusted HTTP requests into admin-only governance endpoints |
| subprocess env -> lock files and temp DB paths | Misconfigured env can cause cross-test lock collisions or accidental use of repo-default state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | T | `tests/support/runtime_drivers.py` | mitigate | Launch the committed scripts as subprocesses with explicit `MEMORY_URL`, `MEMORY_API_KEY`, and per-test lock-file env so HARN-02 cannot be faked by inline helper calls. |
| T-10-02-02 | D | worker/scheduler live tests | mitigate | Reuse `tests/support/waiting.py` polling helpers and pytest timeouts so queue-processing tests fail fast instead of hanging indefinitely. |
| T-10-02-03 | I | governance job state | mitigate | Assert only against temp harness state using `/governance/jobs/{job_id}`, `/metrics`, and `/audit-log`, and keep each live harness instance function-scoped to avoid cross-test state bleed. |
</threat_model>

<verification>
- `uv run pytest -m slow tests/test_harness_foundation_live.py tests/test_runtime_entrypoints_live.py -x`
</verification>

<success_criteria>
- `tests/support/runtime_drivers.py` drives the real worker and scheduler scripts with the shared harness env.
- `tests/test_runtime_entrypoints_live.py` proves enqueue and run-next flows via public HTTP surfaces.
- HARN-02 is covered without inline backend shortcuts or production-only test seams.
</success_criteria>

<output>
After completion, create `.planning/phases/10-test-harness-and-lane-foundation/10-02-SUMMARY.md`
</output>
