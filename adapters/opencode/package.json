{
  "name": "automem-opencode-adapter",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test plugin.test.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "smoke": "npm run build && node smoke.mjs"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^24.6.0",
    "typescript": "^5.9.3"
  }
}
